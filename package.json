{
  "name": "test",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest bench --reporter=json --run ./tests/todo.bench.js",
    "generate": "node ./generator/counter.gen.js",
    "deploy": "node deploy.js"
  },
  "devDependencies": {
    "lighthouse": "^11.7.1",
    "puppeteer": "^22.6.5",
    "vitest": "^1.5.0"
  }
}