{
  "name": "test",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest bench --config ./vitest.config.ts --run ./tests/interact.bench.js",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "lighthouse": "^11.7.0",
    "puppeteer": "^22.6.2",
    "vitest": "^1.4.0"
  }
}
