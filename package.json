{
  "name": "test",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest bench --reporter=json --run ./tests/",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "lighthouse": "^11.7.0",
    "puppeteer": "^22.6.2",
    "vitest": "^1.4.0"
  }
}
