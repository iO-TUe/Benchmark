{
  "name": "test",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest bench --run ./tests/force",
    "generate": "node ./generator/counter.gen.js",
    "results": "node ./generator/results.gen.js",
    "deploy": "node deploy.js"
  },
  "devDependencies": {
    "lighthouse": "^12.0.0",
    "puppeteer": "^22.8.2",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "nodeplotlib": "^1.1.2"
  }
}
